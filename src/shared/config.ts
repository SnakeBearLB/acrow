
// require('dotenv').config();

// export const config = {
// 	cluster: process.env.SOLANA_CLUSTER_URL ?? 'https://api.devnet.solana.com' as string,
// 	keypair: process.env.SOLANA_KEYPAIR as string,
// 	magicApi: process.env.MAGIC_API_KEY as string,
// };

// export default config;